# Factory skill sample

This directory contains a minimal skill description compatible with the
openclaw/claudecode SKILL.md style. The `skill.convert` usage method documents
how cipher converts such skills into capability definitions via `capability.import`.
